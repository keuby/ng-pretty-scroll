import { PrettyScrollDirective } from './pretty-scroll.directive';

describe('PrettyScrollDirective', () => {
  it('should create an instance', () => {
    const directive = new PrettyScrollDirective();
    expect(directive).toBeTruthy();
  });
});
